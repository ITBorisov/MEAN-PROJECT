import { MostLikesPipe } from './most-likes.pipe';

describe('MostLikesPipe', () => {
  it('create an instance', () => {
    const pipe = new MostLikesPipe();
    expect(pipe).toBeTruthy();
  });
});
