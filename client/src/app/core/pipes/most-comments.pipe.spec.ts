import { MostCommentsPipe } from './most-comments.pipe';

describe('MostCommentsPipe', () => {
  it('create an instance', () => {
    const pipe = new MostCommentsPipe();
    expect(pipe).toBeTruthy();
  });
});
